"""
Конфигурация приложения.
Все значения задаются прямо здесь (без .env-файла), но часть секретов можно
переопределить через переменные окружения.

⚠️ Внимание: этот файл содержит секреты (API_HASH, BOT_TOKEN и т.д.).
Не публикуй его и не коммить в открытый репозиторий.
"""
import os

# --- Данные для юзербота (обычный Telegram-аккаунт, от имени которого идёт рассылка) ---
# Получить API_ID и API_HASH: https://my.telegram.org -> API development tools
API_ID = int(os.getenv("TG_API_ID", "38904811"))            # например: 1234567
API_HASH = os.getenv("TG_API_HASH", "a0be67ff4bae5e1766d64d2da45ff7e1").strip()          # например: "abcdef1234567890abcdef1234567890"

SESSION_NAME = "sessions/userbot"

# --- Данные для управляющего бота (обычный бот, созданный через @BotFather) ---
BOT_TOKEN = "8976406641:AAF5zTZAvHJV117zf3K9-pMMFmkupsn5vJA"

# --- Telegram ID root-админов, которые могут входить сразу и выдавать доступ другим людям ---
# Узнать свой ID можно, например, у бота @userinfobot
ADMIN_IDS = [5949150362]   # например: [111111111, 222222222]

# --- Настройки рассылки ---
# Задержка между отправкой сообщений разным пользователям — случайное число секунд
# из интервала [SEND_DELAY_MIN_SECONDS; SEND_DELAY_MAX_SECONDS], чтобы не словить
# flood-limit Telegram и не выглядеть как бот с идеально ровными интервалами.
# Это значения ПО УМОЛЧАНИЮ: их можно поменять прямо из управляющего бота
# (раздел «⚙️ Настройки») без правки этого файла — тогда используется значение
# из базы данных, а не отсюда.
SEND_DELAY_MIN_SECONDS = 2.0
SEND_DELAY_MAX_SECONDS = 4.5

# Путь к базе данных SQLite
DB_PATH = "broadcast.db"

# Часовой пояс для планировщика расписаний (например, Europe/Berlin, Europe/Moscow)
TIMEZONE = "Europe/Berlin"
